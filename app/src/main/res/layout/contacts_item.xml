<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="80dip"
    android:minHeight="80dip">

    <ImageView
        android:id="@+id/avatar"
        android:layout_width="69.33dip"
        android:layout_height="69.33dip"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:layout_marginLeft="6dip"
        android:background="@drawable/head_portrait_bg_big"
        android:contentDescription="@null"
        android:paddingBottom="3dip"
        android:paddingLeft="2dip"
        android:paddingRight="3dip"
        android:paddingTop="2dip"
        android:src="@drawable/ic_default_avatar" />

    <TextView
        android:id="@+id/name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/avatar"
        android:layout_marginLeft="6dp"
        android:layout_marginTop="10dp"
        android:layout_toRightOf="@+id/avatar"
        android:ellipsize="end"
        android:singleLine="true"
        android:textColor="#ff1c1c1c"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/personal_msg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/name"
        android:layout_below="@id/name"
        android:layout_marginTop="10dip"
        android:ellipsize="end"
        android:maxLines="2"
        android:textColor="#ff595959"
        android:textSize="13sp" />

    <TextView
        android:id="@+id/status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_below="@id/personal_msg"
        android:layout_marginRight="6dip"
        android:textColor="#ffce7b01"
        android:textSize="12sp" />

</RelativeLayout>